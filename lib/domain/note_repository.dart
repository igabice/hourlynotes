/*
import 'package:myapp/data/isar_service.dart';
import 'package:myapp/domain/note.dart';

class NoteRepository {
  final IsarService _isarService;

  NoteRepository(this._isarService);

  Future<void> saveNote(Note note) {
    return _isarService.saveNote(note);
  }

  Stream<List<Note>> listenToNotes() {
    return _isarService.listenToNotes();
  }

  Future<void> deleteNote(int id) {
    return _isarService.deleteNote(id);
  }
}
*/
